# 结果验收.md — plan-do-chack.md 执行结果

> 执行日期：2026-08-15 ｜ 执行范围：D:/we-chat/project/miniprogram-11
> 状态标记：PASS / PARTIAL / FAIL / BLOCKED / NOT_RUN（按 plan-do-chack.md 第 14 节格式）

---

## 1. 执行摘要

按 plan-do-chack.md 的 Phase 0 → Phase 3 顺序完成全部需求编号的实施：

- **代码层 P0 修复已完成，但 P0 尚不能全部 PASS**：API-001/API-002、UI-001、DATA-001、DATA-002 已完成代码修复；DATA-003 已改为游标分页并补充失败中断语义，但仍未在真实 CloudBase 跑批验证；SEC-001 仍为 PARTIAL，规则/索引/存储权限尚未在控制台确认。
- **P1 完成情况**：AUTH-001/AUTH-002/TIME-001/ERROR-001/ASSET-001 PASS；SYNC-001 PARTIAL（代码已修复但未做双设备与权限验证）；PERF-001 PARTIAL（业务列表仍有 skip 分页且未做性能观测）；NOTIFY-001 代码链路完成、模板 ID 缺失 → BLOCKED（需公众平台申请）。
- **P2/工程化**：ENG-001 PASS（零依赖脚本 + 48 项测试 + CI）；DOC-001 PASS；ENG-002 手工矩阵 NOT_RUN（需双账号/真机，附完整步骤）。
- 未部署云函数、未修改生产数据库、未提交 git。所有修改仅在工作区。

## 2. 修改文件清单

| 文件 | 变更 |
|:---|:---|
| `miniprogram/utils/dto.js` | **新增**：统一 DTO 转换层（todayList 归一化、dishId 映射、菜单/汇总转换、统计），纯函数可单测 |
| `miniprogram/utils/api.js` | 重写：ApiError(errorCode)、不再自动 toast、includeHidden 参数、notifyApi |
| `miniprogram/utils/util.js` | 新增 showApiError/normalizeJoinCode；删除 getCategoryPlaceholder（ASSET-001） |
| `miniprogram/app.js` | 新增 waitForLogin/retryLogin/refreshUser、登录 ready 态、失败标记、currentFamilyId 以服务端为准 |
| `miniprogram/config.js` | **新增**：订阅消息模板 ID 配置挂载点（留空=通知停用） |
| `miniprogram/pages/menu/menu.js` + `.wxml` | 统一 groups DTO、dishId 映射、真实分页(pageSize=50)、服务端日期、跨午夜刷新、watcher 去抖/去重/重连 |
| `miniprogram/pages/summary/summary.js` + `.wxml` + `.json` | 统一 groups DTO、dishId 撤菜参数、本地移除、下拉刷新开启 |
| `miniprogram/pages/dishes/list/list.js` | includeHidden（chef）、emoji 占位（移除失效资源引用） |
| `miniprogram/pages/dishes/edit/edit.js` | 图片大小/扩展名校验、openid 路径、保存失败/未保存孤儿文件清理 |
| `miniprogram/pages/welcome/welcome.js` + `.wxml` + `.wxss` | 登录等待 + 失败重试入口 |
| `miniprogram/pages/profile/profile.js` + `.wxml` | 通知设置（requestSubscribeMessage + 授权持久化） |
| `miniprogram/pages/family/create|join/manage.js` | 统一 familyId DTO、refreshUser 刷新全局态、错误单次提示 |
| `miniprogram/pages/history/history.js` | dto 归一化、showApiError |
| `cloudfunctions/vote/index.js` | errorCode 信封、cookCount 累计语义、VOTE_ALREADY_EXISTS、第一票通知 ledger 防竞态、密钥环境变量 |
| `cloudfunctions/dish/index.js` | includeHidden（chef 专属）、分页参数校验、图片地址校验、替换/删除旧图清理、统一当日投票清理 |
| `cloudfunctions/family/index.js` | 原子容量闸门、确定性成员 _id、补偿机制、创建者退出保护、解散清理（含图片）、批量联查 |
| `cloudfunctions/dailyReset/index.js` | 幂等历史写入（h_ 派生 _id + set upsert）、分批+失败保留、isHidden 重置窗口、任务日志、手动入口受环境变量保护 |
| `cloudfunctions/notify/index.js` | fail closed（密钥无默认值）、模板 ID 环境变量、家庭/菜品/成员关系校验、脱敏日志、跳转 menu 页 |
| `cloudfunctions/login/index.js` | 用户创建错误区分（where 查询）、统一家庭 DTO（familyId/joinCode/memberCount）、currentFamilyId 自动修正、setNotifyStatus |
| `cloudfunctions/*/package.json` | wx-server-sdk 固定版本 `2.6.3`（ENG-001） |
| `package.json` / `scripts/check-syntax.js` / `scripts/lint.js` | **新增**：ENG-001 检查脚本 |
| `tests/unit/dto.test.js` / `date.test.js` / `tests/contracts/*.test.js` | DTO/日期/加入码、分类筛选、登录首屏及云函数契约（共 48 项测试） |
| `tests/contracts/cloud-functions.test.js` | **新增**：云函数契约静态检查 |
| `.github/workflows/ci.yml` | **新增**：CI（syntax + lint + unit + contracts） |
| `docs/deployment/database.md` + `docs/deployment/security-rules/*.json` | **新增**：SEC-001 规则/索引/存储/触发器/环境变量文档 |
| `uploadCloudFunction.sh` | 修复未定义占位变量，改为 `ENV_ID=... ./uploadCloudFunction.sh` |
| `README.md` / `task-checklist.md` | DOC-001 同步（分类、字段、错误码、未完成功能标注、真实状态） |

## 3. 按需求编号的完成状态表

### Phase 0

| 编号 | 状态 | 关键实现 | 验证 | 测试结果 | 剩余风险 |
|:---|:---|:---|:---|:---|:---|
| API-001 统一今日投票契约 | **PASS** | `dto.normalizeTodayList` 唯一契约 `{date, groups}`；menu/summary 均读 groups；删除 Array.isArray/list 猜测；分组字段 dishId/dishName/voters；服务端返回稳定 errorCode | `npm test`（dto.test.js 12 项）、`node --check` | 12/12 通过 | 未在真机验证渲染；已删除/隐藏菜品以"已删除菜品"兜底展示 |
| API-002 ID 映射 | **PASS** | 展示层统一 dishId（menu `wx:key="dishId"`、summary 撤菜 `data-id={{item.dishId}}`、dish-card 转发已转换对象）；DTO 层完成 `_id`→`dishId` 映射 | dto.test.js 映射用例、契约测试 | 通过 | — |
| UI-001 隐藏菜品闭环 | **PASS** | dish.list 支持 `includeHidden`（服务端校验仅 chef）；管理页可见隐藏菜品并可恢复；隐藏/撤菜/删除共用 `removeTodayVotes`；恢复仅更新 isHidden | 契约测试（includeHidden/PERMISSION_DENIED）、语法检查 | 通过 | 隐藏状态实时刷新依赖 watcher/下拉刷新，未做服务端推送 |
| 汇总页撤菜参数（并入 API-002） | **PASS** | summary.wxml `data-id={{item.dishId}}`，`chefCancel(familyId, dishId)` 传入真实 dishId | 契约测试 | 通过 | — |
| 核心 DTO 转换测试 | **PASS** | tests/unit/dto.test.js 12 用例（归一化/映射/排序/统计/兜底） | `npm run test:unit` | 12/12 | — |
| 语法检查 | **PASS** | scripts/check-syntax.js 全量 `node --check` | `npm run check:syntax` | 32/32 文件通过 | — |

### Phase 1

| 编号 | 状态 | 关键实现 | 验证 | 测试结果 | 剩余风险 |
|:---|:---|:---|:---|:---|:---|
| DATA-001 家庭原子性/幂等 | **PASS** | 加入：原子容量闸门（`memberCount < 10` 条件更新防并发超员）+ 确定性成员 `_id`（`m_{familyId}_{userId}` 天然幂等）+ 三步补偿；创建：确定性 familyId + 分步补偿；退出：创建者非末位拒绝、末位自动解散（成员/菜品/当日投票/历史/通知台账/图片全清理，幂等可重跑）；`family_members` 唯一性由确定性 _id 保证，联合索引清单见部署文档 | 契约测试（原子闸门/幂等/解散清理/创建者保护）、语法检查 | 通过 | 并发超员的真实双账号并发未实测（手工矩阵 NOT_RUN）；成员数由事务内条件更新保证，历史脏数据不自动修复 |
| DATA-002 投票与 cookCount | **PASS** | 确定性投票 `_id`（`v_{date}_{familyId}_{dishId}_{userId}`）；重复返回 `VOTE_ALREADY_EXISTS`；cookCount 明确为**累计被点次数**（仅 add +1；cancel/chefCancel/隐藏/删除/成员退出均不扣减；当日票数以 daily_votes 聚合为准）；取消不再出现负扣减；通知失败不影响主流程且记录日志 | 契约测试（v_ 前缀、无 inc(-1)、VOTE_ALREADY_EXISTS）、语法检查 | 通过 | 历史数据中已被旧逻辑扣减的 cookCount 不回溯修复 |
| DATA-003 dailyReset 幂等 | **PARTIAL** | 历史 `_id` 由投票 `_id` 派生（`h_` 前缀）+ `set` upsert；改为 `_id` 游标分页，避免边删除边 `skip` 漏处理；删除失败会中断任务并保留投票；手动入口 `manualDate` 需环境变量 `ALLOW_MANUAL_RUN=true` | 契约测试（游标分页/h_ 前缀/set/failures/resetWindow/ALLOW_MANUAL_RUN）、语法检查 | 通过 | 定时触发器 Cron 与索引需控制台配置；真实 100+ 条数据、失败重试尚未执行 |
| SEC-001 数据库权限与索引 | **PARTIAL** | 7 集合安全规则文件（`docs/deployment/security-rules/*.json`，读=本人/家庭成员最小化，写=仅云函数）、索引清单、存储安全、触发器文档齐全；`daily_votes` 成员可见读依赖规则 `get()` 能力，退化方案已写入文档 | `npm run lint`（规则 JSON 合法） | 通过 | **控制台未配置**（人工步骤见第 6 节）；若控制台不支持规则 `get()` 跨集合查询，需按文档退化（watcher 改轮询） |
| SEC-002 notify 内部调用 | **PASS** | 密钥仅环境变量 `NOTIFY_INTERNAL_KEY`，代码无默认值、缺失 fail closed；发送前校验菜品归属家庭、受影响用户为当前成员；日志不含密钥/完整 event/用户列表；客户端直调返回 `NOTIFY_FORBIDDEN` | 契约测试（无默认密钥/fail closed/关系校验/跳转页面）、`npm run lint`（硬编码密钥扫描） | 通过 | 需部署时配置环境变量（见第 6 节） |
| STORAGE-001 图片生命周期 | **PARTIAL** | 服务端：imageUrl 仅接受 `cloud://` 且路径含 `/dishes/{familyId}/`；替换或清空图片时删除旧图；删除菜品/家庭解散删关联图；客户端：大小 ≤5MB、扩展名白名单、上传路径含 openid、保存失败/未保存自动清理新图 | 契约测试（validateImageUrl/safeDeleteFiles/清空图片清理）、语法检查 | 通过 | 存储写规则仍需控制台验证；真实上传、替换、删除和失败清理尚未真机执行 |

### Phase 2

| 编号 | 状态 | 关键实现 | 验证 | 测试结果 | 剩余风险 |
|:---|:---|:---|:---|:---|:---|
| AUTH-001 登录 ready 态 | **PASS** | 新增独立登录首屏；`app.waitForLogin()` 可等待；已有家庭用户直达菜单，新用户登录后进入创建/加入家庭；失败显示错误 + 重试按钮；服务端结果覆盖缓存并修正失效 currentFamilyId | 契约测试（登录首屏/currentFamilyId 修正）、语法检查 | 通过 | 微信开发者工具和真机视觉、断网重试仍需人工验证 |
| AUTH-002 资料与家庭 DTO | **PASS** | login/family.list 统一 `familyId` DTO（含 joinCode/memberCount/creatorId）；昵称头像保持默认（未调用隐私授权 API，文档如实标注） | 契约测试（familyId/joinCode/memberCount） | 通过 | 无昵称定制能力（产品取舍，文档已标注） |
| TIME-001 统一业务日期 | **PASS** | 业务日期以 `todayList.date`（服务端东八区）为准；页面 onShow 重算；跨午夜定时重建 watcher + 重载；东八区日期公式有单测 | date.test.js（跨午夜用例）、语法检查 | 通过 | 设备时区异常场景未真机验证 |
| SYNC-001 实时监听成本 | **PARTIAL** | watcher 只监听 `familyId+date`；兼容 CloudBase `dataType=add/update/delete`，保留旧字段兼容；300ms 去抖、在途请求合并、有限重连和下拉刷新兜底 | 语法检查、契约测试 | 通过 | 两台设备真实监听和权限规则尚未验证；若规则 `get()` 不生效需切换轮询 |
| PERF-001 分页/查询/数据量 | **PARTIAL** | 菜单页真实分页（50/页 + 上拉加载）；page/pageSize 服务端类型范围校验；成员/家庭联查改 Promise.all 批量；各列表页具备 loading/空/失败/重试/没有更多状态 | 契约测试、语法检查 | 通过 | 分页仍为 skip（大 offset 游标方案未做，数据量增长后需升级）；性能观测指标（耗时/重载次数）未实现 |
| ERROR-001 统一错误处理 | **PASS** | api.js 不再自动 toast，页面统一 `showApiError`（单次提示）；服务端稳定 errorCode + message；前端按 code 区分（重复/权限/网络/不存在）；失败后可重试 | 契约测试（errorCode 全覆盖）、语法检查 | 通过 | 部分页面错误文案为通用文案（未逐码定制） |
| NOTIFY-001 真实通知链路 | **BLOCKED** | 代码完成：授权入口（profile 通知设置）+ 授权结果持久化（users.notifyStatus/notifyReason）；模板 ID 全部走环境变量（无占位符）；第一票通知 ledger 防竞态（并发安全，不依赖 count==1）；通知范围=当前家庭 chef/实际投票的当前成员；跳转 `pages/menu/menu`；失败不阻塞主流程且可观测 | 契约测试（env 模板/ledger/关系校验）、lint | 通过 | **BLOCKED：模板 ID 需在微信公众平台申请后配置**（人工步骤见第 6 节）；未配置时通知静默停用 |
| ASSET-001 分类占位图 | **PASS** | 删除 `assets/images/categories/*` 无效引用（util.getCategoryPlaceholder 移除），无图菜品统一 emoji 占位（dish-card/list/summary 均已验证） | `npm run lint`（资源引用检查）、语法检查 | 通过 | — |

### Phase 3

| 编号 | 状态 | 关键实现 | 验证 | 测试结果 | 剩余风险 |
|:---|:---|:---|:---|:---|:---|
| ENG-001 自动化验证 | **PASS** | 根 package.json 提供 check:syntax/lint/test/test:unit/check:contracts/predeploy；新增 watcher、游标分页、分类筛选、编辑保存、图片清理、通知台账回归检查；零依赖脚本；CI 提交即检查 | 全部命令实跑 | 见第 4 节 | 自动化仍不等价于 CloudBase 运行级集成测试；dev/test/prod 多环境和正式配置仍需人工确认 |
| ENG-002 手工验收矩阵 | **NOT_RUN** | 矩阵与执行步骤见第 5 节 | — | 未执行 | 需两个微信号/两台设备 + 已部署的云函数环境 |

### P2 文档

| 编号 | 状态 | 关键实现 | 验证 | 测试结果 | 剩余风险 |
|:---|:---|:---|:---|:---|:---|
| DOC-001 文档同步 | **PASS** | README：分类 5 类、isHidden/cookCount、history(date)、错误码表、7 集合（+notify_ledger）、安全设计对齐、未完成功能显式标注；task-checklist：新增真实状态更新段；部署文档与代码一致 | 人工核对 | — | 设计文档 `2026-08-15-*.md` 为历史设计稿，未回写（以 README/代码为准） |

## 4. 自动化测试命令与输出摘要

```bash
$ npm run check:syntax
语法检查通过：32 个文件          # miniprogram + cloudfunctions + scripts + tests 全部 node --check OK

$ npm run lint
Lint 通过：JSON 合法、无硬编码密钥/占位模板、依赖版本固定、资源引用有效

$ npm test
# tests 48, pass 48, fail 0
#  - tests/unit/*.test.js 20 项：todayList/DTO/分类筛选/日期/东八区跨午夜/joinCode/头像等
#  - tests/contracts/cloud-functions.test.js + miniprogram.test.js 28 项：错误码信封/确定性 ID/幂等/游标分页/watcher/图片/通知/权限/登录首屏路由
```

## 5. 手工测试矩阵（ENG-002，均 NOT_RUN——需真实云环境与多账号）

| 场景 | 账号/环境 | 预期 | 状态 | 人工执行步骤 |
|---|---|---|---|---|
| 首次登录 | 新账号 | 展示登录首屏，登录后进入创建/加入家庭页 | NOT_RUN | 开发者工具→清除缓存→编译→观察登录页→点击微信快捷登录；无家庭时不进菜单 |
| 已有家庭冷启动 | 已加入账号 | 直接进入菜单页 | NOT_RUN | 上一场景完成后进入欢迎页点"加入家庭" |
| 登录失败 | 断网 | 明确错误 + 重试入口 | NOT_RUN | 飞行模式→启动→点"重新登录"→恢复网络 |
| 创建家庭 | 账号 A | 创建者为 chef，加入码可用 | NOT_RUN | 欢迎页→创建家庭→填名称→核对"我的-家庭管理" |
| 重复加入 | 账号 B 重复输入同一码 | 不重复创建成员 | NOT_RUN | 加入码页输入→成功→退出→再次输入同一码 |
| 并发加入 | 多账号同时加入 9 人家庭 | 不超过 10 人 | NOT_RUN | 9 人家庭用两台设备同时输入加入码→成员数核对 |
| chef 管理菜品 | 账号 A | 增删改查、隐藏、恢复 | NOT_RUN | 管理页添加→隐藏→列表可见"已隐藏"→恢复→菜单页重新可见 |
| eater 权限 | 账号 B | 不能调用 chef 写操作 | NOT_RUN | eater 尝试进入菜品管理页/长按撤菜→应被拒 |
| 点菜/重复点菜 | A/B 两账号 | 每人每天每菜一票 | NOT_RUN | 双账号各点同一菜→快速连点→daily_votes 仅各 1 条 |
| 取消 | 投票账号 | 只能取消自己的票 | NOT_RUN | A 取消→仅 A 的票消失 |
| 撤菜 | chef | 当日投票清理、其他设备更新 | NOT_RUN | chef 汇总页撤菜→菜单页菜品隐藏、投票清零 |
| 汇总 | 多菜多成员 | 人数/成员/排序正确 | NOT_RUN | 双账号点多道菜→核对汇总页 |
| 实时同步 | 两台设备 | 2 秒内可见变化 | NOT_RUN | 设备 1 点菜→设备 2 菜单/汇总页观察 |
| 历史归档 | dailyReset 手动入口 | 可查询且重复运行不重复 | NOT_RUN | 控制台 dailyReset 配 `ALLOW_MANUAL_RUN=true`，`manualDate=昨日` 连跑两次→vote_history 条数不翻倍 |
| 多家庭隔离 | 同账号多家庭 | 数据不串 | NOT_RUN | 账号 A 加入两家庭→切换后核对菜品/投票隔离 |
| 图片 | 有图/无图/替换/删除 | 加载、降级、清理正确 | NOT_RUN | 无图菜显示 emoji；替换图片后云存储旧文件被删；删除菜品后文件被删 |
| 深色模式 | 系统/手动深色 | 页面可读 | NOT_RUN | 系统切深色→全页面巡检 |
| 跨午夜 | 页面持续打开 | 日期与 watcher 切换 | NOT_RUN | 修改设备时间越过 00:00（或用 `manualDate` 模拟）→ 页面日期与数据刷新 |

## 6. 数据库、存储、定时任务的控制台配置状态（人工待办）

| 项目 | 状态 | 操作位置与步骤（详见 `docs/deployment/database.md`） |
|:---|:---|:---|
| 创建集合（7 个） | 待配置 | 控制台→数据库：`users families family_members dishes daily_votes vote_history notify_ledger` |
| 数据库安全规则 | 待配置 | 将 `docs/deployment/security-rules/*.json` 逐集合粘贴到权限设置→自定义规则；**验证规则 `get()` 跨集合能力**，不支持则按文档退化 |
| 数据库索引（11 项） | 待配置 | 按 database.md 索引清单创建（含 `joinCode` 唯一、`daily_votes` 联合、`families.memberCount`） |
| 云存储写规则 | 待配置 | 粘贴 database.md 第 4 节规则；验证 `indexOf` 支持性 |
| notify/vote 环境变量 | 待配置 | `NOTIFY_INTERNAL_KEY`（强随机值）、`NOTIFY_VOTE_TEMPLATE_ID`、`NOTIFY_CANCEL_TEMPLATE_ID`、`ALLOW_MANUAL_RUN` |
| dailyReset 定时触发器 | 待配置 | Cron `0 0 * * * * *`（东八区每日 0 点） |
| 订阅消息模板 | BLOCKED | 微信公众平台申请两个模板 → 填入环境变量 + `miniprogram/config.js` |
| 安全域名 | 待配置 | 正式发布前配置合法域名 |

## 7. 已知问题和剩余风险

1. **控制台配置是发布前置条件**：数据库规则、索引、触发器、环境变量均未在控制台执行（本任务约束不允许），已给出逐项步骤。
2. **安全规则 `get()` 依赖**：`daily_votes` 成员可见读依赖 CloudBase 规则跨集合查询能力，若控制台不支持，实时监听需退化为轮询（代码已有下拉刷新兜底，文档已写明退化方案）。
3. **cookCount 历史数据**：旧版本取消/退出的扣减逻辑可能已造成部分菜品 cookCount 偏低，新语义（只增不减）上线后不再恶化，但存量数据不回溯。
4. **NOTIFY-001 未完成**：模板 ID 缺失（BLOCKED），通知功能当前实际停用；授权入口已在"我的→通知设置"就绪。
5. **性能**：菜单/菜品分页仍为 skip 分页，家庭菜品量级较大时需升级稳定游标；dailyReset 已改为游标分页；性能观测指标未实现（PERF-001 PARTIAL）。
6. **未真机/双账号验证**：手工矩阵全部 NOT_RUN，MVP 验收前必须完成第 5 节矩阵。
7. **上传路径**：新上传路径含 openid 段，历史图片路径（无 openid 段）仍可正常展示与更新（服务端按 `cloud://` + 家庭路径校验）。

## 8. 本轮代码复核与修复

本轮复核发现并已修复以下代码问题：

1. `dailyReset` 边删除边 `skip` 导致批量数据漏归档，已改为按 `_id` 游标分页；删除失败不再静默吞掉。
2. 菜单/汇总 watcher 已兼容 CloudBase `dataType` 字段及 `delete` 事件。
3. 菜单分类筛选不再追加其他分类的投票分组。
4. 编辑已有菜品时保存按钮根据已有菜名正确启用。
5. 清空菜品图片时服务端会清理旧云存储文件。
6. 点菜通知发送失败时释放通知台账，允许后续请求重试；通知仍受模板 ID 和环境变量配置阻塞。
7. 菜品管理列表加载失败时增加错误提示和重试入口。
8. 新增独立登录首屏：已有家庭用户直达菜单，新用户登录后进入创建/加入家庭流程。

上述修复均通过本地语法检查、lint、单元测试和契约测试；未执行真实 CloudBase 运行验证。

## 9. 是否建议进入 MVP 验收

**暂不建议进入**：代码层已修复本轮发现的问题，但 DATA-003、SEC-001、STORAGE-001、SYNC-001 仍为 PARTIAL，ENG-002 全部 NOT_RUN。必须先完成 CloudBase 控制台配置、真实跑批、双账号/双设备和真机矩阵，再重新验收。

## 10. 是否建议发布

**不建议立即发布**。发布硬性缺口：NOTIFY-001 BLOCKED（模板 ID）、真机测试未执行、隐私协议与用户资料授权说明未配置、线上错误监控/定时任务告警未建立、多环境配置仍需落地。完成上述项后再评估发布。

---

*本文件由执行过程实测生成；所有"通过"均有命令输出为证（见第 4 节），所有 BLOCKED/NOT_RUN 均附原因与人工步骤，未虚报完成。*
